{
    "name": "tb-web-frontend",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --minify",
        "dev": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --watch",
        "check": "tsc --noEmit"
    },
    "devDependencies": {
        "esbuild": "^0.20.0",
        "typescript": "^5.3.0"
    }
}
